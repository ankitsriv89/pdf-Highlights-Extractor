import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/pdf-highlight-frontend/', // Ensure assets are referenced relative to index.html
})
